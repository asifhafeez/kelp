=form_for @restaurant do |f|
      =f.error_messages
      =f.label :name
      =f.text_field :name, :required => "true"
      =f.label :description
      =f.text_field :description, :required => "true"
      =f.label :image
      =f.file_field :image
      =f.submit "Submit"
